services:
  - type: web
    name: captain-blackbeard-voice-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn captainpp:app --host 0.0.0.0 --port 10000
    plan: free
    autoDeploy: true
